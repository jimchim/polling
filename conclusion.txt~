
all changes made to database object using Django's ORM will not take effect 
until save() method is called on the changed object even the default method
provided such as user.change_password() by Django itself will require this step 
